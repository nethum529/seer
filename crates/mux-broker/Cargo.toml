[package]
name = "mux-broker"
version.workspace = true
edition.workspace = true

[dependencies]
constant_time_eq = "0.4"
mux-core = { path = "../mux-core" }
serde = { version = "1", features = ["derive"] }
toml = "0.9"
