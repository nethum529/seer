[package]
name = "seer-runtime"
version.workspace = true
edition.workspace = true

[dependencies]
alacritty_terminal = { version = "0.26.0", default-features = false }
libc = "0.2"
portable-pty = "0.9"
seer-core = { path = "../seer-core" }
seer-net = { path = "../seer-net" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
libc = "0.2"
