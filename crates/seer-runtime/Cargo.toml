[package]
name = "seer-runtime"
version.workspace = true
edition.workspace = true

[dependencies]
alacritty_terminal = { version = "0.26.0", default-features = false }
portable-pty = "0.9"
seer-core = { path = "../seer-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
